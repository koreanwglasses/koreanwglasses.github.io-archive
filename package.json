{
  "name": "fred-choi",
  "version": "1.0.0",
  "private": true,
  "description": "Fred Choi",
  "author": "koreanwglasses",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "format": "prettier --write \"./**/*.{ts,tsx,js,jsx,json}\"",
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "prebuild": "rm -rf docs",
    "build": "gatsby build",
    "postbuild": "mkdir -p docs && cp -r public/* docs && rm -r public",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^3.0.0",
    "gatsby-source-filesystem": "^3.0.0",
    "gatsby-transformer-javascript-frontmatter": "^3.0.0",
    "prettier": "^2.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-icons": "^4.2.0",
    "typescript": "^4.2.2"
  }
}
